export const responseHandler = async (statusCode) => {
  if (statusCode === 200) {
    // vm.$bvToast.toast(message, {
    //   title: "Success",
    //   variant: "success",
    //   solid: true
    // })
  } else {
    // vm.$bvToast.toast(message, {
    //   title: "Error",
    //   variant: "danger",
    //   solid: true
    // })
  }
}